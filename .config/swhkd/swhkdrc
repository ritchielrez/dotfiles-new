XF86Audio{RaiseVolume, LowerVolume, Mute}
    ~/.scripts/changevolume {up, down, mute}

super + e
	nemo

super + w
    librewolf

super + .
    ~/.scripts/rofiemoji

super + t
	~/.scripts/datetime

super + shift + `
    wl-color-picker

super + shift + s
    flameshot gui

Print
    flameshot screen
